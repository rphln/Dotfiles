Include ~/.ssh/config.d/*

# Send only the keys specified in each host.
IdentitiesOnly yes

# Alacritty is non-standard on remote hosts, which causes issues with some
# applications.
SetEnv TERM=xterm-256color
