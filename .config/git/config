[user]
	name = Raphael Nepomuceno
	email = 58113327+rphln@users.noreply.github.com

[url "ssh://git@github.com/"]
	insteadof = https://github.com/

[alias]
	history = log --pretty=format:'%Cred%h %Creset%s %Cgreen(%ar)%Cblue%d'
	show-last = log --max-count 1 --patch

	fix = "!f() { git commit --allow-empty-message --no-edit && exec git rebase --interactive; }; f"

	unstage = reset HEAD
	uncommit = reset --soft HEAD^

	exec = "!exec "

[diff]
	algorithm = histogram

[pull]
	rebase = true

[fetch]
	prune = true

[rebase]
	autostash = true
	autosquash = true

[commit]
	verbose = true

[diff "md"]
	xfuncname = "^#.*"

[diff "ipynb"]
	textconv = pandoc --standalone --to markdown
